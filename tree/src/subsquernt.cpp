#include <iostream>
#include "tree.h"


// DESC: 真二叉树
//   零个叶子节点或者两个叶子节点
//  node->left == nullptr && nullptr == node->right || node->left != nullptr node->righ != nullptr
class Solution {
public:
    std::vector<std::vector<TreeNode*>> real_binary_tree(int n) {
        return {

        };
    }
};
